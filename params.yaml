data:
  target_column: price
  drop_columns:
    - flat_id
    - building_id

split:
  test_size: 0.25
  random_state: 42

model:
  type: RandomForestRegressor
  n_estimators: 50
  max_depth: 20
  random_state: 42
  n_jobs: -1
